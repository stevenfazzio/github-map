# Required
GITHUB_TOKEN=           # GitHub personal access token (for GraphQL API)
CO_API_KEY=             # Cohere API key (embeddings + topic modeling)
ANTHROPIC_API_KEY=      # Anthropic API key (README summarization + topic naming)

# Optional (only needed for 00_enumerate_repos.py)
GCP_PROJECT=            # Google Cloud project ID (for BigQuery)
